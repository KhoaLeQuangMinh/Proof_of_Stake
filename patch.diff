--- src/main/java/network/NetworkEngine.java
+++ src/main/java/network/NetworkEngine.java
@@ -211,6 +211,7 @@
                 // MUST RE-SIGN because ledgerTipHash is part of getSignableData()!
                 handshake.setSignature(Ed25519Util.signToBase64(myPrivateKey, handshake.getSignableData()));
             }
+            // System.out.println("[DEBUG-HANDSHAKE-SEND] listenPort=" + listenPort + " rawJson=" + handshake.toJson());
             writer.println(handshake.toJson());
 
             String line;
